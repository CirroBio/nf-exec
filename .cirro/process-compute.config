process {
    withName: EXEC {
        // The shared default is errorStrategy 'retry' with maxRetries 2. A user
        // command is not guaranteed to be idempotent, so a retry can double-write
        // into the output dataset.
        errorStrategy = 'terminate'
        maxRetries    = 0
    }
}
